{
  "name": "@appliance.sh/bootstrap",
  "version": "1.54.2",
  "private": true,
  "description": "Shared bootstrap orchestrator for the Appliance CLI and Desktop shells",
  "license": "MIT",
  "author": "Eliot Lim",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch --preserveWatchOutput",
    "clean": "rm -rf ./dist",
    "typecheck": "tsc --noEmit",
    "dev:setup": "echo 'pnpm workspaces handle linking automatically'",
    "test": "echo \"@appliance.sh/bootstrap: no unit tests yet (exercised via helper/cli); add vitest specs here when ready\""
  },
  "dependencies": {
    "@appliance.sh/infra": "workspace:*",
    "@appliance.sh/sdk": "workspace:*",
    "@aws-sdk/client-ecr": "^3.1005.0",
    "@aws-sdk/credential-providers": "^3.1044.0",
    "@pulumi/pulumi": "^3.216.0"
  },
  "devDependencies": {
    "@types/node": "24.12.2",
    "typescript": "^5.7.3"
  }
}
