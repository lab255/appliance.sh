{
  "name": "@appliance.sh/install-aws",
  "version": "1.54.2",
  "private": true,
  "description": "Install the Appliance platform on AWS",
  "repository": {
    "type": "git",
    "url": "https://github.com/appliance-sh/appliance.sh.git",
    "directory": "packages/install-aws"
  },
  "license": "MIT",
  "author": "Eliot Lim",
  "scripts": {
    "build": "mkdir -p ./dist && cdk synth > dist/appliance-install-aws.yml",
    "deploy": "cdk deploy --all",
    "deploy:direct": "cdk deploy --all -- direct",
    "clean": "rm -rf ./dist/",
    "test": "echo \"@appliance.sh/install-aws: no unit tests yet (CDK synth is exercised by build); add cdk assertion tests here when ready\""
  },
  "dependencies": {
    "@appliance.sh/sdk": "workspace:*",
    "@aws-sdk/client-cloudfront": "^3.955.0",
    "aws-cdk": "^2.1033.0",
    "aws-cdk-lib": "^2.230.0",
    "constructs": "^10.4.3"
  },
  "devDependencies": {
    "@types/node": "24.12.2"
  }
}
